{
    "require": {
    },
        "require-dev": {
            "heroku/heroku-buildpack-php" : "*"
        },
        "post-install-cmd": [
            "php artisan clear-compiled",
            "chmod -R 777 public/"
        ]
    }
